#!/bin/bash
# Per-scenario overrides for scenario_15_et_arabidopsis (HPC).
# BRAKER mode: ET (RNA-seq only via pyVARUS, no proteins — BRAKER1-style benchmark)

export BRAKER4_SKIP_OPTIMIZE_AUGUSTUS=1
export BRAKER4_RUN_OMARK=0

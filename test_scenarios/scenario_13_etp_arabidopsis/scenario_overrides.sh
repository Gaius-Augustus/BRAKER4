#!/bin/bash
# Per-scenario overrides for scenario_13_etp_arabidopsis (HPC).

# Explicitly confirm benchmark settings (also set in shared config.ini).
export BRAKER4_SKIP_OPTIMIZE_AUGUSTUS=1
export BRAKER4_RUN_OMARK=0

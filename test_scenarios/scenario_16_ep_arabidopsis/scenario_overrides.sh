#!/bin/bash
# Per-scenario overrides for scenario_16_ep_arabidopsis (HPC).
# BRAKER mode: EP (proteins only, no RNA-seq — BRAKER2-style benchmark)

export BRAKER4_SKIP_OPTIMIZE_AUGUSTUS=1
export BRAKER4_RUN_OMARK=0
